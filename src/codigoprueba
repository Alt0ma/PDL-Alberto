let i int = 11;        			// variable entera
let j int = 10;        			// variable entera
let st string = "Hola";         // variable cadena 
let resultado boolean = false;  // variable logica
let p string = 'Voy por comillas simples';

if (j<i) {
    boolean=true;
    i=i+j;
}